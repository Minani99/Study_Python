isinstance() / type()

str() -> int Class 내에서는 숫자를 문자로 바꾸는거로 정의가 되어있음

*클래스 변수 << 클래스 내부에 있지만 [self가 X] (공용)
*인스턴스 변수 << (전용)

-------------------

public >> 공용
private >> 내부적으로만 사용, 비공개

-------------------

상속 후 오버라이딩 ?
기본 메서드를 상속받은 후 자식 클래스에서 해당 메서드를
새로운 기능으로 덮어쓰기