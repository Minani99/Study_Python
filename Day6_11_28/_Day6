#item >> 딕셔너리 내부 키 : 밸류들을 얻는 함수
clear 도있지

enumerrate() >> 열거함수

이터러블 >> 반복가능한 객체
순차적인 값을 반환할 수 있는 구조

이터레이터 : 반복을 실제 수행하는 객체

next() 함수 출력을 할 수 있다 정도
순차적으로 다음 값을 반환
이거 사용시 stopIteration 더 이상 반환 할 값 없다
한번 순회를 마치면 다시 조회 불가

이터러블 vs 이터레이터

testlist=["500","600","700"] 이터러블 객체
print(next(testlist)) testlist는 이터러블 이지만 이터레이터는 아님
testiter = iter(testlist) >> 이터러블 객체에서 이터레이터로 변환 iter()