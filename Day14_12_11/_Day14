람다: def로 작성 함수를 줄여서 작성하는 방식

콜백함수: 함수의 매겨밴수에 사용하는 함수

#filter() -> filter(함수, 리스트)
#map() -> map(함수, 리스트)

제너레이터 >> 리스트로 변경가능

파일객체 = open(문자열: 파일경로, 문자열: 읽기모드)
open함수의 첫 매개변수는 파일경로
두번쨰 매개변수 모드

w 쓰기
a append
r 읽기

*1
리스트컴프리헨션
그냥 해석하는대로

*2
제너레이터컴프리헨션
제너레이터를 사용하여 job함수 4번실행
한번에 하나씩 ㄷ값을 생성,계산함
next() 호출하면 하나씩 계산
메모리낭비 줄일려고
각 함수의 실행은 필요할때 next()를 호출함
지연실행